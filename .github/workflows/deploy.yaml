# This workflow will deploy your HTML/PHP project to your hosting server via FTP
# whenever you push to the 'main' branch or run it manually.

name: Deploy to my repo2

# ---- TRIGGERS ----
on:
  push:                  # Trigger when code is pushed
    branches:
      - main             # Only when pushing to the 'main' branch
  workflow_dispatch:     # Allow running manually from the GitHub Actions tab

# jobs:
#   ftp-deploy:            # Name of the job
#     runs-on: ubuntu-latest  # The environment where this job will run

#     steps:
#       # Step 1: Get the latest code from your repository
#       - name: Checkout repository
#         uses: actions/checkout@v4

      # Step 2: Upload files to your hosting server via FTP

      # - name: Upload via FTP
      #   uses: SamKirkland/FTP-Deploy-Action@v4.1.0
      #   with:
      #     server: ftp://your-host.com      # Your FTP host (replace this)
      #     username: ${{ secrets.FTP_USERNAME }} # Stored securely in GitHub Secrets
      #     password: ${{ secrets.FTP_PASSWORD }} # Stored securely in GitHub Secrets
      #     local-dir: .                     # Local directory to upload ('.' means whole repo)
      #     remote-dir: /path/on/server      # Path on the server where files go
